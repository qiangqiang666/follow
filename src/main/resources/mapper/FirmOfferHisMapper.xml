<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.monkey.follow.mapper.FirmOfferHisMapper">
    <select id="selectByMsgId" resultType="com.monkey.follow.model.FirmOfferHis">
         select * from t_his  where msg_id = #{msgId} limit 1
    </select>
</mapper>